# REST channel - dùng cho Gradio/Web UI gọi API
rest:

# Socket.IO - dùng nếu muốn real-time với web frontend
# socketio:
#   user_message_evt: user_uttered
#   bot_message_evt: bot_uttered
#   session_persistence: true

# Rasa X / Enterprise (nếu dùng)
# rasa:
#   url: "http://localhost:5002/api"
